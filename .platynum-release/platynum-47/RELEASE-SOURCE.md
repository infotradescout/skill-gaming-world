# Platynum-47 Windows release snapshot

- Product source: `infotradescout/platynum-47`
- Canonical source commit: `e966a79fe9b103be308f140a9969781611bc36bf`
- Purpose: temporary, private Windows installer forge only.

This hidden snapshot exists because the canonical repository's GitHub Actions release run was blocked before any job started by GitHub's synthetic `BuildFailed` workflow state. It does not run Platynum, receive project files, or expose the local workspace through Skill Gaming World.

Do not make product changes here. Make them in the Platynum-47 repository, then replace this snapshot from its reviewed commit. This source set was verified byte-for-byte against the canonical commit before this release branch was created.
